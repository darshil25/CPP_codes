/* Worst case = highest time taken by algorithm = Big O Notation = O()
    average Case = Average time taken by algorithm = Big Theta Notation = Θ()
    Best case = Lowest time taken by Algoritm = Big Omega Notation = Ω()

    For Calculating time complexity where loop run by nth time then its sign is o()

    o(log n) < o(n^1/2) < o(n) < o(n2) < o(n3)
    */
##include<iostream> using namespace std;

int main()
{

    return 0;
}