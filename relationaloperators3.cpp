/*BITWISE OPERATORS
-> & (and)
-> | (or)
-> ^ (XOR) = jo bey operands ni value same hase to 0 return thase ne alag alag hase to 1.
-> ~ (ones) = 0 input kariye to 1 return ape ne 1 kariye to 0
-> << (left shift) = number na bits ne left ma shift kare
    ex.4<<1 (0100) = 1000 [<<x ahi x key ke ketla place nu shift karvanu 6 ]
    equation - a<<n = a x 2^n
-> >> (right shift) = number na bits ne right ma shift kare
    ex.4>>1 (0100) = 0010 [>>x ahi x key ke ketla place nu shift karvanu 6 ]
    equation - a>>n = a / 2^n
    */